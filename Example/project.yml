# Название проекта
name: ReactiveDataDisplayManagerExample
options:
  # Версия Xcode
  xcodeVersion: 11.3.1
  # Создание пустых директорий
  generateEmptyDirectories: true
  # Создание групп
  createIntermediateGroups: true
targets:
    ReactiveDataDisplayManagerExample:
      dependencies:
        - sdk: UIKit.framework
      type: application
      platform: iOS
      deploymentTarget: 11.0
      scheme:
        configVariants: all
        testTargets: [ReactiveDataDisplayManagerExampleTests]
      sources:
        - path: ReactiveDataDisplayManager
      info:
        path: Tricolor/Application/Info/Debug/Info.plist
        properties:
          CFBundleName: ReactiveDataDisplayManagerExample
          CFBundleShortVersionString: $(MARKETING_VERSION)
          CFBundleVersion: $(CURRENT_PROJECT_VERSION)
      settings:
        base:
          PRODUCT_BUNDLE_IDENTIFIER: ru.surfstudio.rddm.example
          MARKETING_VERSION: "7.0.0"
          CURRENT_PROJECT_VERSION: 0
          VERSIONING_SYSTEM: "apple-generic"
          DEBUG_INFORMATION_FORMAT: dwarf-with-dsym
          LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/Frameworks @loader_path/Frameworks"
          CODE_SIGN_IDENTITY: "iPhone Developer"
          CODE_SIGN_STYLE: Automatic

    ReactiveDataDisplayManagerExampleTests:
      type: bundle.unit-test
      platform: iOS
      deploymentTarget: 11.0
      scheme:
        configVariants: all
      sources: [ReactiveDataDisplayManagerTests]
      info:
        path: ReactiveDataDisplayManagerTests/Info.plist
        properties: []
      dependencies:
        - target: ReactiveDataDisplayManagerExample
      settings:
        configs:
          Debug:
            EXCLUDED_ARCHS[sdk=iphonesimulator*]: "arm64"
